---
interface Props {
  items: string[];
}

const { items } = Astro.props;
---
{items.length ? (
  <ul class="toolbox-list">
    {items.map((item) => (
      <li>{item}</li>
    ))}
  </ul>
) : null}
